spring:
  application:
    name: library-client
#  cloud:
#    config:
#      discovery:
#        service-id: config-service
#        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_ADDRESS:localhost:8070}/eureka
